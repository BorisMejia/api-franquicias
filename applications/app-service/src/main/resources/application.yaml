server:
  port: 8080
spring:
  application:
    name: "api-franquicias"
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: "/h2"
  profiles:
    include: null
adapters:
  r2dbc:
    host: ${AWS_HOST_DB:localhost}
    port: "5432"
    database: ${AWS_DATABASE_NAME:franquicia_db}
    username: ${AWS_USERNAME_DB:franquicia_user}
    password: ${AWS_PASSWORD_DB:franquicia_pass}
  data:
    r2dbc:
      repositories: null
      enabled: true
spring-doc:
  api-docs:
    path: "/v3/api-docs"
  swagger-ui:
    path: "/swagger"
management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      probes:
        enabled: true
cors:
  allowed-origins: "http://localhost:4200,http://localhost:8080"
